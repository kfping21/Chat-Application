# CLAUDE.md — AI 辅助开发规则文件

## 项目规则

### 技术栈
- 前端：Android（Kotlin）
- 后端：Node.js
- 数据库：MySQL
- 部署：单机最小化部署（Node.js + MySQL）

### 目录结构
- `frontend/`：安卓端工程与说明（Kotlin）
- `backend/src/`：Node.js 后端代码
- `backend/sql/`：数据库初始化脚本
- `docs/`：项目文档（API/架构/数据库）

### 代码规范
- Android 端使用 Kotlin + MVVM（推荐）
- 网络请求统一封装（如 Retrofit/OkHttp 封装层）
- 后端 API 路由统一前缀与统一响应结构
- API 响应使用统一格式

### 禁止事项
- 不要在 Android 端直接进行数据库直连
- 不要在后端硬编码密钥、密码
- 不要跳过输入参数校验与错误处理
- 不要修改配置文件（除非明确要求）

## AI 执行约束

- 优先复用现有组件与工具函数，避免重复造轮子
- 修改前先阅读相关模块上下文，避免破坏既有行为
- 新增接口时同步更新 `docs/api.md` 与客户端调用层
- 涉及行为变更时，更新对应文档（API/架构/说明）
- 默认保持向后兼容，除非需求明确要求破坏性变更
