<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_deep"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:fitsSystemWindows="true">

    <!-- Navigation Host -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph" />

    <!-- Custom Bottom Navigation Bar - Improved with ConstraintLayout -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/bottom_nav_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="@dimen/bottom_nav_margin_bottom"
        android:elevation="16dp"
        android:clipChildren="false"
        android:clipToPadding="false"
        android:fitsSystemWindows="true">

        <!-- Background bar -->
        <View
            android:id="@+id/custom_bottom_bar"
            android:layout_width="0dp"
            android:layout_height="@dimen/bottom_nav_height"
            android:background="@drawable/bg_bottom_nav_v2"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- Tab buttons container -->
        <LinearLayout
            android:id="@+id/tabs_container"
            android:layout_width="0dp"
            android:layout_height="@dimen/bottom_nav_height"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <!-- Tab: 首页 -->
            <LinearLayout
                android:id="@+id/tab_feed"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:paddingStart="8dp"
                android:paddingEnd="8dp">

                <ImageView
                    android:id="@+id/tab_feed_icon"
                    android:layout_width="@dimen/icon_nav"
                    android:layout_height="@dimen/icon_nav"
                    android:src="@drawable/ic_nav_home"
                    app:tint="@color/gold_primary" />

                <TextView
                    android:id="@+id/tab_feed_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:text="@string/nav_feed"
                    android:textSize="10sp"
                    android:textColor="@color/gold_primary"
                    android:textStyle="bold"
                    android:includeFontPadding="false" />
            </LinearLayout>

            <!-- Transparent divider to prevent accidental touches -->
            <View
                android:layout_width="1dp"
                android:layout_height="@dimen/nav_tab_divider_height"
                android:background="@color/divider_transparent" />

            <!-- Tab: 遇见 -->
            <LinearLayout
                android:id="@+id/tab_explore"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:paddingStart="8dp"
                android:paddingEnd="8dp">

                <ImageView
                    android:id="@+id/tab_explore_icon"
                    android:layout_width="@dimen/icon_nav"
                    android:layout_height="@dimen/icon_nav"
                    android:src="@drawable/ic_nav_explore"
                    app:tint="@color/text_muted" />

                <TextView
                    android:id="@+id/tab_explore_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:text="@string/nav_explore"
                    android:textSize="10sp"
                    android:textColor="@color/text_muted"
                    android:includeFontPadding="false" />
            </LinearLayout>

            <!-- Center spacer for FAB - fixed width instead of weight -->
            <View
                android:layout_width="56dp"
                android:layout_height="1dp" />

            <!-- Tab: 回响 -->
            <LinearLayout
                android:id="@+id/tab_echoes"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:paddingStart="8dp"
                android:paddingEnd="8dp">

                <ImageView
                    android:id="@+id/tab_echoes_icon"
                    android:layout_width="@dimen/icon_nav"
                    android:layout_height="@dimen/icon_nav"
                    android:src="@drawable/ic_nav_echoes"
                    app:tint="@color/text_muted" />

                <TextView
                    android:id="@+id/tab_echoes_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:text="@string/nav_echoes"
                    android:textSize="10sp"
                    android:textColor="@color/text_muted"
                    android:includeFontPadding="false" />
            </LinearLayout>

            <!-- Transparent divider to prevent accidental touches -->
            <View
                android:layout_width="1dp"
                android:layout_height="@dimen/nav_tab_divider_height"
                android:background="@color/divider_transparent" />

            <!-- Tab: 我的 -->
            <LinearLayout
                android:id="@+id/tab_my"
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center"
                android:clickable="true"
                android:focusable="true"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:paddingStart="8dp"
                android:paddingEnd="8dp">

                <ImageView
                    android:id="@+id/tab_my_icon"
                    android:layout_width="@dimen/icon_nav"
                    android:layout_height="@dimen/icon_nav"
                    android:src="@drawable/ic_nav_my"
                    app:tint="@color/text_muted" />

                <TextView
                    android:id="@+id/tab_my_label"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="3dp"
                    android:text="@string/nav_my"
                    android:textSize="10sp"
                    android:textColor="@color/text_muted"
                    android:includeFontPadding="false" />
            </LinearLayout>

        </LinearLayout>

        <!-- Center Action (Post) FAB - overlaid on top -->
        <LinearLayout
            android:id="@+id/fab_post_wrapper"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_gravity="center_horizontal|top"
            android:gravity="center_horizontal"
            android:layout_marginTop="-8dp"
            android:elevation="8dp"
            android:clipChildren="false"
            android:clipToPadding="false"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <!-- The Actual Circular Button -->
            <FrameLayout
                android:id="@+id/fab_post"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:background="@drawable/bg_fab_gradient">

                <ImageView
                    android:layout_width="22dp"
                    android:layout_height="22dp"
                    android:layout_gravity="center"
                    android:src="@drawable/ic_nav_post"
                    app:tint="#0A0E14" />
            </FrameLayout>

            <!-- 发布 label -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:text="@string/nav_post"
                android:textSize="10sp"
                android:textColor="@color/gold_primary"
                android:textStyle="bold"
                android:includeFontPadding="false" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
